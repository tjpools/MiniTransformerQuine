# Analysis Workspace

This directory contains reverse-engineering and inspection workspace material used to study build outputs and binary structure.

The `ghidra/` subtree stores Ghidra project artifacts for that analysis workflow.
